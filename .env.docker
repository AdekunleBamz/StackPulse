# Docker-specific environment variables
# Use in production deployments

# Target build stage (dev or runner)
TARGET=runner
NODE_ENV=production

# Server URLs
NEXT_PUBLIC_SERVER_URL=http://server:3001

# Redis
REDIS_URL=redis://redis:6379
