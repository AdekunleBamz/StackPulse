# Badge metadata URI fallback note

Badge support checks should verify that metadata links open even if the primary app route is unavailable.

Record the fallback host or static metadata source used for release validation.

This keeps earned badge troubleshooting separate from wallet mint confirmation issues.
