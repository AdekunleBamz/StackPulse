# Metric card skeleton note

Keep metric-card loading states in visual QA so charts and counts do not jump when live stats arrive.

## Checklist

- Throttle the stats API and watch the initial dashboard render.
- Confirm skeleton dimensions match the loaded metric cards.
