# Alert history empty range note

When an alert history query returns no rows, capture the selected range, network, and account address prefix.

The empty state should distinguish no matching alerts from a failed server request.

Use this note when triaging dashboard reports after a quiet monitoring period.
