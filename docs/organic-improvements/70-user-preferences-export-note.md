# User preferences export note

Keep account preference export behavior in support notes so alert tuning can be reconstructed when users report unexpected delivery.

## Checklist

- Confirm exported preferences include channel, threshold, and quiet-hours settings.
- Exclude secrets and webhook signing values from exported support bundles.
