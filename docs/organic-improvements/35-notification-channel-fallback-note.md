# Notification Channel Fallback

- Configure fallback channel order for failed primary delivery.
- Log fallback transitions with reason codes.
- This increases alert delivery reliability.

- Log the fallback reason when channels fail over to preserve delivery context.
