# Shared Package Publish Check

- Validate exported types and API compatibility before publish.
- Block publish when generated types are stale.
- This keeps workspace packages in sync.

- Verify exported type surfaces before publishing a shared package release.
