# Dashboard stale metric badge

Add a dashboard QA note for marking stale metric cards when the backing query is older than the refresh window.

The badge should identify stale data without hiding the last known value.
