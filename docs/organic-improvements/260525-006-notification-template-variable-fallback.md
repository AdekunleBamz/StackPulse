# Notification template variable fallback

Track a copy review for notification templates when a variable is missing from the alert payload.

Fallback text should be obvious to operators and avoid sending placeholder tokens to subscribers.
