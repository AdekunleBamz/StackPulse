# WebSocket Reconnect Window

- Use bounded reconnect windows for websocket clients.
- Expose retry count in diagnostics for incident triage.
- This keeps real-time streams resilient without infinite loops.

- Simulate packet loss in staging to validate reconnect timing before mainnet pushes.
