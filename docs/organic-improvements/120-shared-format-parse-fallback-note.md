# Shared Format Parse Fallback

## Summary
Shared formatting helpers should define fallback output for invalid dates, amounts, and addresses.

## Checks
- Test invalid values in frontend, server, and shared package usage.
- Confirm fallbacks do not look like real chain data.
- Keep helper docs aligned with tests.
