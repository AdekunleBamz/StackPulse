# Server Startup Preflight

- Run config and dependency preflight checks before server boot.
- Fail fast with actionable messages for any missing requirements.
- This prevents partial startup states.

- Fail startup early when core dependencies do not pass preflight probes.
