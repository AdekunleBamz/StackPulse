# Dependency Update Note

- Upgrade one workspace package group at a time.
- Re-run relevant tests after each dependency batch.
- Document any runtime or API compatibility shifts.
- Keep lockfile and package edits in same commit.
