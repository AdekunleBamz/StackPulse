# Badge image alt fallback

Add an accessibility check for reputation badge images when metadata does not include alt text.

The fallback should use the badge name and tier rather than a generic image label.
