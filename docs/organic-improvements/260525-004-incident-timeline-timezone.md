# Incident timeline timezone

Document a release check that incident timeline entries display a consistent timezone label.

This avoids mixing local browser time with UTC timestamps during escalation reviews.
