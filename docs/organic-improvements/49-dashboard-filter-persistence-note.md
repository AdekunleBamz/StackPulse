# Dashboard Filter Persistence

- Persist dashboard filter selections per user session.
- Clear persisted filters on explicit reset action.
- This improves repeat monitoring workflows.

- Clear persisted filters when schema keys change to avoid hidden mismatch bugs.
