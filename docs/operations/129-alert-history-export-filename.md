# Alert History Export Filename

Maintenance check: Keep alert history export filenames timestamped and filter-aware.

Follow-up note: Confirm exported filename filters match the visible filter chips.

Follow-up note: Recheck export filenames after adding saved filter presets.
