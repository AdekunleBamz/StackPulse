/**
 * Frontend Utilities
 * Common utility functions used across the frontend application.
 */

export { truncateAddress, isValidStacksAddress, getAddressNetwork, isSameAddress, normalizeStacksAddress, isContractAddress, extractDeployerAddress, extractContractName } from './address';